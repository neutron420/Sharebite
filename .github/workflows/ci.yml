# name: Sharebite CI

# on:
#   workflow_dispatch:

# concurrency:
#   group: sharebite-ci-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   app-build-and-test:
#     name: App Build And Test
#     runs-on: ubuntu-latest
#     timeout-minutes: 25
#     env:
#       DATABASE_URL: postgresql://user:password@localhost:5432/sharebite?sslmode=disable
#       JWT_SECRET: ci-jwt-secret
#       R2_ACCOUNT_ID: ci-r2-account
#       R2_ACCESS_KEY_ID: ci-r2-access-key-id
#       R2_SECRET_ACCESS_KEY: ci-r2-secret-access-key
#       R2_BUCKET_NAME: sharebite
#       R2_PUBLIC_DOMAIN: https://example.r2.dev
#       NEXT_PUBLIC_MAPBOX_TOKEN: ci-mapbox-token
#       REDIS_URL: redis://localhost:6379
#       INTERNAL_WS_URL: http://localhost:8081
#       NEXT_PUBLIC_WS_URL: ws://localhost:8080
#       NEXT_PUBLIC_APP_URL: http://localhost:3000
#       OPENAI_API_KEY: ci-openai-key
#       RESEND_API_KEY: ci-resend-key
#       NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY: ci-turnstile-site-key
#       CLOUDFLARE_TURNSTILE_SECRET_KEY: ci-turnstile-secret-key
#       GROQ_API_KEY: ci-groq-key
#       RAPIDAPI_KEY: ci-rapidapi-key
#       RAPIDAPI_HOST: google-api31.p.rapidapi.com
#       RESEND_FROM_EMAIL: support@example.com
#       RAZORPAY_KEY_ID: rzp_test_ci
#       RAZORPAY_KEY_SECRET: ci-razorpay-secret
#       RAZORPAY_WEBHOOK_SECRET: ci-razorpay-webhook-secret
#       NEXT_PUBLIC_RAZORPAY_KEY_ID: rzp_test_ci
#       GRAFANA_CLOUD_PROM_REMOTE_WRITE_URL: https://prometheus-prod-xx-prod-ap-south-1.grafana.net/api/prom/push
#       GRAFANA_CLOUD_PROM_USERNAME: ci-prom-username
#       GRAFANA_CLOUD_API_KEY: ci-grafana-key
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Bun
#         uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: latest

#       - name: Install dependencies
#         run: bun install --frozen-lockfile

#       - name: Generate Prisma client
#         run: bunx prisma generate

#       - name: Build app
#         run: bun run build

#       - name: Run tests
#         run: bun run test

#   k8s-manifest-validate:
#     name: Kubernetes Manifests
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup kubectl
#         uses: azure/setup-kubectl@v4

#       - name: Render dev overlay
#         run: kubectl kustomize k8s/overlays/dev > /tmp/sharebite-dev.yaml

#       - name: Render prod overlay
#         run: kubectl kustomize k8s/overlays/prod > /tmp/sharebite-prod.yaml

#   terraform-validate:
#     name: Terraform Validate
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3

#       - name: Terraform init (no backend)
#         working-directory: terraform
#         run: terraform init -backend=false

#       - name: Terraform validate
#         working-directory: terraform
#         run: terraform validate
